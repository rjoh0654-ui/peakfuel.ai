import React from "react";
import ReactDOM from "react-dom/client";
import PeakFuelWebsite from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PeakFuelWebsite />
  </React.StrictMode>
);
